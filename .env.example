# Warehouse
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ipl
POSTGRES_USER=ipl
POSTGRES_PASSWORD=changeme

# dbt uses the same vars
DBT_PROFILES_DIR=./warehouse

# Optional — only needed if using CricketData.org for upcoming fixtures
CRICKETDATA_API_KEY=

# MLflow tracking (Phase 6)
MLFLOW_TRACKING_URI=file:./mlruns
