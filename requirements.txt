# Streamlit Cloud requirements — dashboard runtime only.
# pyproject.toml has the full project deps (training, ingestion, dbt, etc).
# This file mirrors only what the deployed dashboard actually imports.
streamlit>=1.38
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
xgboost>=2.0
plotly>=5.18,<7
